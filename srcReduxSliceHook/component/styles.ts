import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1, 
        justifyContent: 'center',
        padding: 40
    },
    text: {
        textAlign: 'center',
        padding: 10,
        fontWeight: '600',
        fontSize: 20
    },
    list: {
        padding: 20,
        marginVertical: 10, 
    }
})